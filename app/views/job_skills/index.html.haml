%h1 Job Skills
%table
  %tr
    %th Name
    %th Version
    %th{colspan: '3'} Actions
  - @sorted_skills.each do |skill|
    %tr
      %td= skill.name
      %td= skill.version
      %td= link_to 'Show', skill
      %td= link_to 'Edit', edit_job_skill_path(skill)
      %td= link_to 'Destroy', skill, method: :delete, |
        data: {confirm: 'Are you sure?'}      |

%br/
=link_to "New Skill", new_job_skill_path